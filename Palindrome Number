class Solution {
public:
    bool isPalindrome(int x) {
        int c=0;
        long int sum=0,temp;       
        temp=x;
        while(x>0)
        {
            c=x%10;
            x/=10;
            sum=(sum*10)+c;
            
        }
        return sum==temp;
    }
};
